/**
 * * Array of routes.
 * @exports
 */
export var routes = [
    // ? {groupBy: '/api', middleware: 'auth', routes: [
    // ?     {name: 'index', get: ['/', 'WebController@index']},
    // ? ]},
    {name: 'index', get: ['/', 'WebController@index']},
];